import 'package:flutter/material.dart';

class Promo {
  const Promo(this.badge, this.title, this.subtitle, this.icon, this.color);

  final String badge;
  final String title;
  final String subtitle;
  final IconData icon;
  final Color color;
}
